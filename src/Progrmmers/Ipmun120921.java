
/*
package Progrmmers;

public class Ipmun120921 {
    public static void main(String[] args) {
        String A = "hello";
        String B = "ohell";
        String AA = A+A;
        boolean in = AA.contains(B);
        int count = 0;
        StringBuilder sb = new StringBuilder();
            if (!in) {
                System.out.println("-1");
            } else {
                while (true) {
                    char End = AA.charAt(AA.length() - 1);
                    String another = AA.substring(0, AA.length() - 1);
                    sb.append(End).append(another);
                    if (AA.substring(0, A.length() - 1).equals(B.substring(0, B.length()))) {
                        break;
                    } else {
                        sb.setLength(0);
                        count++;
                    }
                }
            }
        System.out.println(count);
        }
        }
        */


